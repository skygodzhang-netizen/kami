# 感知接口工具箱约束规范（Perception Interface Toolbox Constraints）

> **文档状态**：v1.3 · 2026-08-12（v1.0 建立；v1.1 新增 §4.1 错误处理分层细则；v1.2 引入身份标签体系 + 层级术语防护；v1.3 新增 §1.5 能力基线）
> **动机**：工具节点升级（toolnode Rust 化 + BusyBox 兜底）后，感知接口在 SKILL.md 资源索引中的归类与架构文档脱节（曾归入"映射层节点工具箱"），且旧 CLI 工具箱一节已被 toolnode 取代。本文档为独立后的"感知接口工具箱"定义完整的约束体系。
> **配套修订**：SKILL.md 资源索引（映射层工具箱只留人格层；新建感知接口工具箱；删除过时 CLI 工具箱节）
> **v1.1 补充**：依据"错误处理分层模型"完善红线约束——执行态错误处理（感知接口拥有）vs 语义态错误决策（主循环/映射层专属）vs 学习闭环（记录层专属），消除"无自主推理 = 无错误处理能力"的误解。红线总纲见 §4。
> **v1.2 补充**：引入统一身份标签体系（`[成员]` / `[内部]` / `[构建]` / `[遗留]`）应用于全文档及 SKILL.md / architecture.md，并固化"层级术语防护"声明，杜绝内部层级被误读为框架层级。
> **v1.3 补充**：新增 §1.5 能力基线——基于 2026-08-12 实测验收（31/31）记录工具注册表 / toolnode 23 子命令 / 治理能力 / 能力边界，作为能力变更的对照基准。

---

## 0. 定位声明（最重要的一条）

**感知接口工具箱是框架次循环"感知接口（Tool Use）层"的文档落地，与映射层平级，不隶属于映射层。**

- 架构依据：architecture.md §6（次循环 = 6.1 映射层 + 6.2 感知接口）
- 归类依据：tool_use_spec.md 职责边界红线（被动能力执行器）
- **工具箱独立 ≠ 架构层独立**：工具箱只是文档归类手段，信息流约束完全沿用架构文档，不发明任何新流

---

## 1. 成员约束（身份标签体系）

### 1.0 身份标签定义（全文档统一 · 一眼识别层级身份）

所有层级表述出现处，组件名一律带统一前缀标签；**无标签 = 非工具箱组件**：

| 标签 | 含义 | 可替换性 | 对外可见 |
|---|---|---|---|
| `[成员]` | 工具箱正式成员（对外契约载体） | 不可随意替换 | ✅ 调用方直接面对 |
| `[内部]` | 工具箱内部实现（非平级工具箱） | 可随时替换 | ❌ 仅经 PerceptionNode 路由 |
| `[构建]` | 构建期组件（非运行时） | — | ❌ 不参与运行 |
| `[遗留]` | 已废弃（禁止新代码依赖） | 待清理 | ❌ 仅兼容旧记录 |

> **层级术语防护**：工具箱内部的"统一入口层 / 能力层 / 兜底层"为**内部路由表述**，不构成框架架构层级（框架层级仅指最外圈 / 外环 / 次循环）。任何跨文档引用须附带此声明。

### 1.1 [成员] 正式成员（工具箱主体）

| 成员 | 职责 | 定位 |
|---|---|---|
| `scripts/perception_node.py` | 感知接口统一入口（PerceptionNode） | 唯一正式成员，工具箱的"门面" |

### 1.2 [内部] 内部实现（非平级工具箱成员）

以下组件**不是**独立的工具箱，而是 perception_node.py 的内部路由目标（能力层 + 兜底层）：

| 组件 | 职责 | 路由角色 |
|---|---|---|
| `scripts/toolnode`（Linux ELF） | 主路径能力层 | Rust 二进制主路径 |
| `scripts/toolnode.exe`（Windows PE） | 主路径能力层 | Rust 二进制主路径 |
| `scripts/toolnode.py` | 能力层 Python 原型 | Rust 二进制缺失时的回退 |
| `scripts/busybox_fallback.py` | 保命兜底层 | Rust + Python 全缺时保命 |

**约束**：
- ✅ 调用方只面对 `perception_node.py`（或经其 `call_tool` 接口），**不直接**调用 toolnode/busybox
- ✅ 内部实现可随时替换（Rust ↔ Python ↔ BusyBox），不影响工具箱对外契约
- ❌ 禁止把内部实现列为与 perception_node.py 平级的"工具箱"

### 1.3 [构建] 构建期组件

| 组件 | 职责 | 说明 |
|---|---|---|
| `scripts/toolnode-rs/`（已移至工作空间备份） | Rust 源工程 | Cargo.toml + src/，仅用于编译 toolnode 二进制，非运行时依赖；备份于 `agi-evolution-model-backup/toolnode-rs` |

### 1.4 [遗留] 已废弃成员（已清理 · 2026-08-12）

> 4 个散装 CLI 与 cli-tools-guide.md 已于 2026-08-12 移入 `_deprecated_backup/` 目录（原位置无任何引用）。如需回退，可从备份目录恢复。

| 文件 | 状态 | 说明 |
|---|---|---|
| ~~`scripts/cli_file_operations.py`~~ | 已清理 | 已被 toolnode fs 取代，无代码引用 |
| ~~`scripts/cli_system_info.py`~~ | 已清理 | 已被 toolnode sys 取代，无代码引用 |
| ~~`scripts/cli_process_manager.py`~~ | 已清理 | 已被 toolnode proc 取代，无代码引用 |
| ~~`scripts/cli_executor.py`~~ | 已清理 | 已被 toolnode exec 取代，无代码引用 |
| ~~`references/cli-tools-guide.md`~~ | 已清理 | 仅服务于上述 4 个 CLI 的文档 |

**约束**：
- ❌ 新代码禁止 import 或依赖这 4 个遗留模块
- ✅ 备份保留于 `_deprecated_backup/`（确认无回归后可由用户手动删除）

---

## 1.5 能力基线（当前能力全景 · 2026-08-12 实测验收）

> 本节记录感知接口工具箱**当前实际具备**的能力，作为能力变更的对照基准。任何增删改须在此更新，并经 §6 Q1-Q8 验收。
> 数据来源：TOOL_REGISTRY 实测 + verify_step5_e2e.py 31/31 验收（2026-08-12）。

### 1.5.1 工具注册表（TOOL_REGISTRY · 5 项）

| 工具 | 状态 | 能力 | 后端 |
|---|---|---|---|
| `toolnode` | ✅ 真实 | 系统交互四组 23 个子命令（见 1.5.2） | Rust 二进制主路径 |
| `calculator` | ✅ 真实 | 数学计算 | C 扩展（core_perception_node） |
| `web_search` | ⚠️ STUB | 搜索网络信息（未实现） | 拦截返回 STUB_NOT_IMPLEMENTED |
| `get_weather` | ⚠️ STUB | 获取城市实时天气（未实现） | 同上 |
| `search_documents` | ⚠️ STUB | 搜索文档数据库·支持分页（未实现） | 同上 |

> **约束**：STUB 工具注册但不进主路径，是预留扩展位；实现时须移除 stub 标记并过 Q1/Q2 验收。

### 1.5.2 toolnode 子命令（23 个操作）

| 组 | 操作 | 说明 |
|---|---|---|
| **fs**（9） | read / write / list / copy / move / delete / mkdir / stat / search | 文件系统全操作；写入回读验证 + 来源时间戳（防静默失败） |
| **sys**（7） | cpu / mem / disk / net / uptime / env / all | 系统信息采集 |
| **proc**（6） | list / search / detail / kill / tree / stats | 进程管理 |
| **exec**（1） | run | 受控命令执行（危险命令网关拦截） |

### 1.5.3 治理能力（统一入口层 · 标准投送口）

| 能力 | 实现 | 实测依据 |
|---|---|---|
| Trace ID 全链路 | 每次调用生成并强制注入，toolnode 尊重注入值 | Q3a/Q3b PASS |
| 参数校验 | Schema 单一事实来源（TOOL_REGISTRY），缺参/未知 op → INVALID_PARAMS | Q2 PASS |
| 缓存策略 | ToolCache 执行态复用（get/set/clear） | 类方法在列 |
| 重试机制 | `_no_retry` 确定性错误码集合，预算内机械重试 | Q5 PASS |
| 可观测性 | total/success/fail/cache/retry 指标导出（get_metrics） | Q7 PASS |
| 错误台账 | 内部状态支撑重试判定；错误事件上报记录层（上报≠入库） | 红线三合规 |
| 流式响应 | call_tool_with_streaming（SSE 模拟） | 方法在列 |
| 分页支持 | search_documents 预留 | 方法在列 |
| Token 预估 | 内置能力 | 方法在列 |
| 版本控制 | ToolConfig + 契约版本 | 模块结构 |

### 1.5.4 能力边界（诚实声明）

- 3 个 STUB 桩工具未实现（web_search / get_weather / search_documents）
- 感知节点是**被动执行器**：不决策、不推理、不主动发起调用（红线约束，见 §4）
- C 扩展双平台（.pyd / .so），加载失败自动降级纯 Python
- BusyBox 能力边界：fs 核心 + proc.list + sys.all + exec.run；不支持 → BUSYBOX_UNSUPPORTED

---

## 2. 信息流约束（与架构文档完全一致）

```
  映射层（决策） ⇄ 感知接口工具箱 ⇄ 能力层（toolnode/busybox 内部路由）
                      │
                      ├→ 记录层 · 错误智慧库（仅上报错误事件）
                      └→ 主循环（仅响应工具调用请求）
```

| # | 起点 | 终点 | 数据 | 方向 | 依据 |
|---|---|---|---|---|---|
| F1 | 映射层 | 感知接口 | 行动指令（工具名/参数/执行控制） | **双向** | architecture.md §6.1/6.2（唯一合法双向流） |
| F2 | 感知接口 | 映射层 | 结构化感知数据 | **双向** | 同上 |
| F3 | 主循环（数学/自我迭代） | 感知接口 | 工具调用请求、调试指令 | 单向进 | information-flow.md §3 |
| F4 | 感知接口 | 主循环 | 清晰无噪音数据 | 单向出 | 同上 |
| F5 | 感知接口 | 记录层（错误智慧库） | 错误事件上报 | 单向出 | tool_use_spec.md 红线三（上报≠入库） |
| F6 | 感知接口 | 能力层（toolnode/busybox） | 子命令调用 | 内部 | 提案 §3（工具箱内部路由，非框架层信息流） |

### 2.1 禁止的信息流

| ❌ 禁止 | 原因 |
|---|---|
| 感知接口 → 记录层 JSON 轨直接写库 | 记录层数据只来源于三顶点（architecture.md 信息流约束） |
| 感知接口 → 最外圈（意向性模组） | 最外圈只与建议池交互 |
| 感知接口主动发起调用 | 被动执行器：调用与否由映射层决定（tool_use_spec.md 红线一） |
| 感知接口对结果做语义判断/二次决策 | 只能按预置规则机械响应（红线一） |
| 感知接口修改调用方意图（"我以为你想要的"） | 红线一 |

### 2.2 信息流验证流程（实现任何数据操作前执行）

```
步骤1：识别起点 / 终点 / 数据类型 / 方向
步骤2：在 §2 表中查找——合法则继续，非法则拒绝
步骤3：检查 §2.1 禁止清单——命中则拒绝
步骤4：实现代码时添加强制标注（发送者/接收者/数据类型/方向）
```

---

## 3. 契约约束（统一契约，贯穿全链路）

所有经感知接口工具箱的数据交换必须遵循统一契约：

```json
{
  "status": "success|error",
  "data": { },
  "error": null,
  "metadata": { "duration_seconds": 0.12, "trace_id": "..." },
  "trace_id": "...",
  "timestamp": "2026-08-12T00:00:00"
}
```

| # | 约束 | 说明 |
|---|---|---|
| C1 | trace_id 全链路贯穿 | 每次调用由 PerceptionNode 生成并强制注入，toolnode 尊重注入值 |
| C2 | 参数 schema 单一事实来源 | `toolnode.SCHEMAS`（toolnode.py 导出）收归 `TOOL_REGISTRY`，调用前强制校验 |
| C3 | 防静默失败 | 写入类 → 回读磁盘确认，返真实路径+字节+sha256；检索类 → 返来源 mtime（非响应时刻） |
| C4 | 边界归一化 | toolnode/busybox 的 status-based 契约 → PerceptionNode 内部 success-based，边界统一转换 |
| C5 | 错误契约不误判崩溃 | 合法 JSON 错误契约（如 COMMAND_TIMEOUT）按业务结果处理；仅 stdout 非合法契约时才视为崩溃 |

---

## 4. 红线约束（被动能力执行器 · 不可逾越）

> 详见 tool_use_spec.md 职责边界红线章节（1731-1768 行）。此处为工具箱视角的浓缩。
>
> **红线总纲（先读这条）**："完全无自主推理"约束的只是感知接口的**语义思考**（对结果的"意味着什么、下一步怎么办"的研判），**不是**禁止它处理错误。错误处理的关键不在"想"，而在"报得清楚 + 查表准确"：
> - **执行态错误处理**（感知接口拥有，红线内允许）：确定性错误码识别、查 `_no_retry` / `retryable` 表、预算内机械重试、结构化错误契约上报——本质是 if-else + 查表，不是思考
> - **语义态错误决策**（主循环/映射层专属）：理解上下文、分析根因、决策降级路径（换工具/降精度/上报人工）、是否入库——需要全局视野与历史经验，属于思维
> - **学习闭环**（记录层专属）：错误入库 → 根因分析 → 生成预防规则 → 下次调用前前置检查
>
> 三层分工是错误处理的**完整形态**。若让感知接口"自主推理"，它只能看到局部（无上下文/无历史/无人格），产生的是**残缺思考**，比机械响应更危险，且会架空主循环、断裂学习闭环。**红线不是在约束能力，是在防止降级。**

### 红线一：禁止自主推理与决策
- ❌ 不决定"该不该调用某工具"
- ❌ 不对结果做语义判断或二次决策（"这个结果不对，我应该重试/换个工具"——不允许）
- ❌ 不修改调用方意图
- ❌ 不跨环伸手（不替记录层/自我迭代做进化/复盘/智慧库写库等决策）
- ✅ 允许：按**预置规则**机械响应（`_no_retry` 集合、确定性错误码、`retryable` 标志、预算内重试）——这是执行态能力，不是语义思考

### 红线二：最小化自我表述
- ✅ 只输出结构化契约 + operational telemetry（trace_id / backend / duration）
- ✅ 进程内 logger 可输出执行态日志（`using rust backend`），不进入契约
- ❌ 禁止"我认为/我决定/我治理/我进化"等主体性措辞
- ❌ 禁止对调用方解释超出错误码+消息范畴的原因

### 红线三：错误台账归属
- ✅ 感知接口内部可维护**只读错误台账**（支撑重试判定与可观测）
- ✅ 可向上层智慧库接口**上报**错误事件（单向依赖、不反向约束）
- ❌ 台账不得被宣称为框架"错误智慧库"的写入动作
- ❌ 入库/复盘/预防决策在记录层完成，感知接口不参与

---

## 4.1 错误处理分层细则（执行态 / 语义态 / 学习闭环）

> 本细则消除"无自主推理 = 无错误处理能力"的误解。错误处理是三层分工的闭环，任何一层缺失都是残缺的错误处理。

### 4.1.1 执行态错误处理（感知接口拥有 · 红线内允许）

感知接口**必须**具备且完整实现的执行态能力（这是它的职责，不是越权）：

| # | 能力 | 实现形态 | 边界 |
|---|---|---|---|
| E1 | 确定性错误码识别 | 查 `_no_retry` 集合（COMMAND_TIMEOUT / DANGEROUS_COMMAND_BLOCKED / BUSYBOX_* 等） | 只识别、不解释"为什么" |
| E2 | 预算内机械重试 | 按预置规则（格式/参数失败 2 次立即重试；超时指数退避 1s/2s/4s；语义错误 2 次带反思轨迹） | 预算由上层预置，感知接口只执行 |
| E3 | 结构化错误契约上报 | 错误码 + message（事实级描述）+ trace_id + metadata | 上报 ≠ 入库；不携带建议性/叙事性内容 |
| E4 | 兜底路由触发 | 按 §5 触发条件机械判断（B1-B4） | 触发与否是查表，不是判断 |
| E5 | 防静默失败 | 写入回读验证、检索返来源时间戳 | 验证动作，非语义判断 |

**判据**：一项动作是否属于执行态，看它是否满足"**输入确定性错误信号 → 查表/规则 → 输出确定性响应**"。满足即为执行态（允许）；不满足即为语义态（禁止）。

### 4.1.2 语义态错误决策（主循环 / 映射层专属）

以下能力**不属于**感知接口，越权即违反红线一：

| # | 决策 | 归属 | 说明 |
|---|---|---|---|
| S1 | 根因分析 | 主循环（数学顶点） | "为什么失败"需要上下文与推理 |
| S2 | 降级路径选择 | 主循环 / 映射层 | 换工具 / 降精度 / 上报人工——需要全局视野 |
| S3 | 是否入库 / 复盘 / 预防 | 记录层 | 聚合多源错误后决策（红线三） |
| S4 | 调整重试预算 | 上层（自我迭代顶点） | 感知接口只消费预算，不修改预算 |
| S5 | 人格化处理错误 | 映射层 | 基于马斯洛需求 × 大五人格的决策 |

**感知接口在该层的角色**：只提供**足够结构化**的错误信息，让上层能快速决策——"报得清楚"本身就是它的核心价值。

### 4.1.3 学习闭环（记录层 · 错误智慧库）

```
错误发生
  → 感知接口执行态处理（E1-E5）
  → 结构化上报（E3）
  → 记录层：入库 → 根因分析 → 生成预防规则（Phase 3 规则自动生成）
  → 预防规则回流 → 下次调用前前置检查（error_wisdom_prevention）
```

- ✅ 感知接口受益于预防规则（前置检查减少错误），但**不参与**规则生成
- ✅ 错误从"临时处理"进化为"系统预防"——这是三层分工独有的优势

### 4.1.4 红线边界示例（可执行判断）

| 场景 | 感知接口该做 | 感知接口禁止做 |
|---|---|---|
| toolnode 返回 COMMAND_TIMEOUT | 查 `_no_retry` → 不重试 → 上报错误契约 | 自行决定"换 exec 用 bash 重试" |
| fs.write 回读不一致 | 返回 verified=False + 错误契约 | 自行决定"重写一次或换路径" |
| 工具连续失败 3 次 | 按预置规则上报，附 trace_id 与失败次数 | 自行决定"放弃该工具，改用备用工具" |
| 参数校验失败 | 返 INVALID_PARAMS（schema 规则） | 自行推断"用户想要的参数"并改写 |

---

## 5. 兜底约束（三级路由）

```
主路径（Rust 二进制 toolnode）→ 回退（Python 原型 toolnode.py）→ 保命（BusyBox）
```

| # | 触发条件 | 兜底动作 |
|---|---|---|
| B1 | `_toolnode_binary_path()` 返回 None（Rust + Python 都缺） | 直接 BusyBox |
| B2 | subprocess spawn 失败 / 超时（OSError/TimeoutExpired） | BusyBox |
| B3 | stdout 非合法契约（非 JSON / 无 status） | BusyBox |
| B4 | 合法错误契约（exit 1 + 合法 JSON） | **不**触发兜底（业务结果） |

**约束**：
- ✅ BusyBox 能力边界须明确（不支持的操作返 `BUSYBOX_UNSUPPORTED`，不静默假装成功）
- ✅ BusyBox 契约与主路径一致，`metadata.backend="busybox"` 标记可观测
- ✅ 确定性错误（COMMAND_TIMEOUT 等）进 `_no_retry` 集合，不盲目重试

---

## 6. 质量门禁约束（Q1-Q8 八维验收）

感知接口工具箱的任何变更须通过以下验收（脚本：`scripts/verify_step5_e2e.py`，13/13 基准）：

| # | 维度 | 验收标准 |
|---|---|---|
| Q1 | 真实接线 | 注册工具全部真实实现；stub 显式标注且不入主路径 |
| Q2 | 参数校验 | Schema 强制校验，缺参/未知 op → INVALID_PARAMS |
| Q3 | trace_id 贯穿 | 全链路注入并回流，无死代码 |
| Q4 | 防静默失败 | 写入回读磁盘验证；检索返来源+时间戳 |
| Q5 | 重试/兜底 | 确定性错误不重试；BusyBox 兜底真实生效 |
| Q6 | 错误闭环 | 真实工具失败上报智慧库（before/after 计数验证） |
| Q7 | 可观测性 | 延迟/成功率/缓存命中可追踪可导出 |
| Q8 | 跨平台一致 | Windows/Linux 双二进制行为一致 |

---

## 7. 演进约束

- 🔧 **小步、可验证、可回退**：任何改动先提案 → 用户确认 → 小步落地（铁律，源自提案 §0）
- 🔧 **改动边界**：工具箱内部实现可替换；对外契约（统一契约 + 信息流约束）**不可破坏**
- 🔧 **回归验证**：改动后必须重跑 `verify_toolnode_step2.py`（Q1-Q6）或 `verify_step5_e2e.py`（Q1-Q8）
- 🔧 **术语统一**：本工具箱场景下，"治理层 = 感知接口 = 统一入口层"是同一组件的三种称呼，文档中可互换但不得造成歧义
- 🔧 **文档同步**：SKILL.md 资源索引与架构文档脱节即视为缺陷，须同步修订

---

## 8. 参考文档

- [architecture.md](architecture.md) §6 — 次循环架构（感知接口与映射层平级）
- [tool_use_spec.md](tool_use_spec.md) — 感知接口工具规范 + 职责边界红线
- [information-flow.md](information-flow.md) — 次循环信息流
- [tool-node-redesign-proposal.md](tool-node-redesign-proposal.md) — 工具节点重规划（三级路由）
- [behavior-baseline.md](behavior-baseline.md) — 行为基线（契约优先/防静默失败）
- ~~[cli-tools-guide.md](cli-tools-guide.md)~~ — 遗留 CLI 工具参考（已清理，备份于 `_deprecated_backup/`）

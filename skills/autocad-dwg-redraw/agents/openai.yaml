interface:
  display_name: "AutoCAD DWG Redraw"
  short_description: "Rebuild and validate AutoCAD DWG drawings."
  default_prompt: "Use $autocad-dwg-redraw to generate a drawing-specific redraw prompt, rebuild the source DWG, and validate entity, annotation, layer, block, and dimension fidelity."

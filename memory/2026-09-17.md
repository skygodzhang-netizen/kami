# 2026-09-17

## CUA / Win-CAD-Node 进展（2026-09-15 ~ 2026-09-17）

### 已确认状态
- Win-CAD-Node (192.168.100.109) 当前 `paired · connected`，version 2026.9.4
- node describe 的 `caps` 已包含 `computer`、`screen`（完整 caps: browser, computer, file, local-inference, mcp, screen, system）
- 之前节点端未声明 CUA 能力的根因：Gateway 侧 `gateway.nodes.pluginTools.enabled` 与 `cua-computer` 插件、`tools.alsoAllow` 未正确生效；修复并批准 surface 后（requestId 6f606678-83b9-42c0-abd4-bf64d681256e，`openclaw nodes approve`）已生效
- CUA 组件文件已确认存在于 Windows 端：`C:\Users\11561\AppData\Roaming\npm\node_modules\openclaw\dist\computer-tool-D8FqCW1c.mjs`
- 未重启 Gateway、未改 trustedProxies/Caddy/model/provider/timeout、未修改 Windows 端 node.cmd

### 2026-09-17 用户请求
- 用户要求"打开我电脑上的记事本"，即再次要求执行 CUA 实机测试（打开 Notepad）
- 04:38 那轮开始执行：确认节点在线 + screen/computer caps 已注册 → 执行桌面截图（screen 工具，成功）→ 准备打开记事本（computer 工具）
- 04:48 该轮被中断（turn aborted），CUA 实机测试未完成到"打开记事本"这一步

### 待完成
- CUA 实机测试序列（打开 Notepad → 点击 → 输入 "OpenClaw CUA TEST" → 截图确认 → 关闭 Notepad）尚未完整跑完，因 04:48 turn 中断
- 需要用户再次要求时继续，或下一轮主动补完
# 2026-09-17

## CUA / Win-CAD-Node 进展（2026-09-15 ~ 2026-09-17）

### 已确认状态
- Win-CAD-Node (192.168.100.109) 当前 `paired · connected`，version 2026.9.4
- node describe 的 `caps` 已包含 `computer`、`screen`（完整 caps: browser, computer, file, local-inference, mcp, screen, system）
- 之前节点端未声明 CUA 能力的根因：Gateway 侧 `gateway.nodes.pluginTools.enabled` 与 `cua-computer` 插件、`tools.alsoAllow` 未正确生效；修复并批准 surface 后（requestId 6f606678-83b9-42c0-abd4-bf64d681256e，`openclaw nodes approve`）已生效
- CUA 组件文件已确认存在于 Windows 端：`C:\Users\11561\AppData\Roaming\npm\node_modules\openclaw\dist\computer-tool-D8FqCW1c.mjs`
- 未重启 Gateway、未改 trustedProxies/Caddy/model/provider/timeout、未修改 Windows 端 node.cmd

### 2026-09-17 用户请求
- 用户要求"打开我电脑上的记事本"，即再次要求执行 CUA 实机测试（打开 Notepad）
- 04:38 那轮开始执行：确认节点在线 + screen/computer caps 已注册 → 执行桌面截图（screen 工具，成功）→ 准备打开记事本（computer 工具）
- 04:48 该轮被中断（turn aborted），CUA 实机测试未完成到"打开记事本"这一步

### 待完成
- CUA 实机测试序列（打开 Notepad → 点击 → 输入 "OpenClaw CUA TEST" → 截图确认 → 关闭 Notepad）尚未完整跑完，因 04:48 turn 中断
- 需要用户再次要求时继续，或下一轮主动补完

## 12:00 记忆写入（CAD Hybrid Agent 生产状态）
- 新建 `memory/cad-hybrid-agent-state.md`（完整状态文档：架构链路、Win-CAD-Node 生产状态、CUA 结论、CAD Hybrid 方案流程、控制优先级、已验证能力、Timeout 经验、后续行为规则）
- `MEMORY.md` 追加 `# 2026-09-17` 章节（摘要 + 链接到详细文档）
- git commit + push：`f9f7bb3 记忆写入：CAD Hybrid Agent 生产状态` → origin/master
- 用户确认：记忆迁移完成，不要重新迁移/扫描/重写；SQLite memory 路径：`/home/ubuntu/.openclaw/memory/main.sqlite.migrated`

## 12:04 CAD 任务执行模式确认
- 用户下发：CAD 任务直接按生产链路执行，不重查节点/不重测 CUA/不动 timeout/不改架构
- 收到 CAD 任务路由：需求分析 → CAD 规划 → CAD Executor → AutoCAD 生成 → 文件验证 → 返回结果

## 12:04-12:08 CAD 任务
- **法兰盘任务**（φ100，6 孔）：CAD Executor（ezdxf）生成 `cad-test/flange.dxf`（DXF R2010，18.9KB）；几何校验通过（圆 9 个、直线 8 条，6 螺栓孔在 PCD R40 上）；节点侧 CUA 验证未做（Windows 平台 file.write 不在 allowlist）
- **100×50 矩形任务**：ezdxf 生成 `cad-test/rect_100x50.dxf`（LWPOLYLINE + 2 中心线 + 标注，结构校验通过）

## 12:09 用户反馈：策略错误
- 用户指出：CAD 任务不应降级到 ezdxf 本地生成，应走真实 AutoCAD 执行流程
- 用户要求修改 CAD 任务判断逻辑（路由规则）

## 12:32-12:52 路由规则修正
- 更新 `cad-hybrid-agent` skill：
  - "MANDATORY Routing"：触发词（打开AutoCAD/电脑绘图/AutoCAD操作/截图验证/模拟人工）→ 强制走 Win-CAD-Node + AutoCAD
  - ezdxf 定位：允许文件检查/几何验证/后处理，禁止替代 AutoCAD 执行
  - 新增"ezdxf 永远不替代 AutoCAD 执行"的明确禁令
- 用户确认路由规则已修正（13:05 只读检查通过）

## 12:56 CAD 强制验收任务（100×50 矩形）
- 用户下发强制验收：必须真实打开 AutoCAD、COM API 绘图、DXF 保存、screen.snapshot 验证
- 执行结果：**FAIL**
  - Win-CAD-Node 网关侧确认在线（paired/connected，5 caps）✅
  - `computer.act` 命令持续报 `DriverError.Tool` / `arguments could not be classified by plugin cua-computer` ❌
  - `exec host=node` shell 通道：cmd 环境，PowerShell 管道/复杂语法被截断（`'grep' 不是内部或外部命令`，code 255）❌
  - AutoCAD COM 脚本已 base64 打包（2732 字符）并提交 exec host=node，但无脚本输出证据 ❌
  - DXF 文件路径：`C:\OpenClaw-CAD-Test\rect_100x50.dxf`（节点侧无法确认是否真实生成）❌
  - screen.snapshot 未执行 ❌

## 13:27 生产链最终修复任务
- 用户下发：修复 CAD Hybrid Agent 实际执行链，不生成报告，必须真实验证
- 任务范围：
  1. 修复 Win-CAD-Node Endpoint（检查是否有旧 192.168.100.108:18789 配置）
  2. 确认 Node 执行环境
  3. 修复 AutoCAD 执行链（COM API）
  4. 执行真实 CAD 验收任务（100×50 矩形）
- 已知失败点（13:27 任务执行时）：
  - `exec host=node` 节点侧 shell 通道不可用（cmd 环境，复杂语法截断）
  - CUA `computer.act` 持续 DriverError
  - 端点配置检查无法完成
- 任务未完成（中断/未完成全链路）

## 13:05 CAD 路由规则只读确认
- cad-hybrid-agent skill "MANDATORY Routing" 规则已加载 ✅
- ezdxf 定位：文件检查/后处理允许，禁止替代 AutoCAD 执行 ✅
- 下次 CAD 软件操作任务强制调用 Win-CAD-Node + AutoCAD ✅

## 当前系统状态（截至 13:32）
- CAD Hybrid Agent skill 已加载，路由规则已修正
- Win-CAD-Node 网关侧：paired · connected，5 caps
- CUA computer.act：持续 DriverError（未解决）
- exec host=node：节点侧 cmd 环境，复杂语法限制（未完全解决）
- CAD 端到端生产链路：**FAIL**（AutoCAD 启动证据不完整，CUA 截图验证缺失）
- 待解决：① exec host=node shell 转义；② cua-computer 插件 DriverError；③ 节点侧端点配置检查

## Git 提交记录
- `f9f7bb3` 记忆写入：CAD Hybrid Agent 生产状态（12:00）
- 12:32 前提交（100×50 矩形 + 法兰盘 ezdxf 文件）→ 用户指出策略错误，这些不应作为最终结果
- 后续提交：待修复完成后执行

## 系统监控模式 (启动 14:06:45 UTC)
- 模式：只监控不修复；配置/Gateway/Skill/Memory/Node 修改全部由外部调试 AI 执行
- Gateway: local wss://127.0.0.1:18789 reachable 0ms; systemd user running (pid 215784, active) ✅
- Win-CAD-Node: paired · connected (2h ago), caps: browser/computer/file/local-inference/mcp/screen/system ✅
- Agent: 本会话响应正常，无 timeout，无任务中断
- CAD Skill: cad-hybrid-agent 已加载；Memory: MEMORY.md + cad-hybrid-agent-state.md 存在
- 已知遗留故障（供外部调试，本监控不修复）:
  1. CUA computer.act DriverError.Tool (launch_app/list_apps) / get_window_state 参数无法分类
  2. exec host=node 节点 shell 环境复杂命令转义问题（纯 cmd 简单命令可用）
  3. 旧端点 192.168.100.108:18789 启动入口未定位（node.vbs 在 C:\Users\11561 不存在）
  4. AutoCAD COM ModelSpace Null 待验证（acad.exe 13:52 确认在运行, PID 21120, Session 2）

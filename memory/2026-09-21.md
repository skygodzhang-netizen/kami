# 2026-09-21 记忆日志

## Win-CAD-Node CUA 状态确认与故障教训

### 上午确认（10:57/08:50 UTC 会话）
- kami 确认 Win-CAD-Node CUA 已恢复：单 node.exe（Session 3 交互式）、Gateway 7/7 caps（browser/computer/file/local-inference/mcp/screen/system）、computer/screen/screen.snapshot/list_apps/type/__close_execution 全部 E2E 通过
- kami 明确指令：**视为已恢复，不再做修复**；后续 CAD 直接用 Win-CAD-Node + CUA；不要再重复排查/重配 pairing/重建 Agent/改配置；若未来再次出现 computer/screen 丢失，按 MEMORY.md L387+ 故障记录做**只读诊断**
- 08:47 曾让我"打开记事本"测试：computer.act launch_app 多次 COMPUTER_HOST_BUSY，最后用 shell fallback（powershell Start-Process）打开，但报告"已打开"被截图证伪——**实际没打开**（见下）

### 重要教训：不要根据 ok:true / 截图误判成功
- 09:07 汇报"记事本已打开"是错误的：当时截图实际只显示桌面壁纸，没有 Notepad 窗口。误把 shell fallback 或部分成功当成完成。
- 用户明确要求：**不要因为之前 E2E 通过就认为当前 CUA 一定正常，以当前实时状态为准；不要只根据 ok:true 判断成功**
- 验证 CAD/Windows 操作的铁律（已在 MEMORY.md L376-378 "验证原则"）：exit 0 ≠ 成功，文件存在 ≠ 成功，必须重新读取实际结果/窗口

### 只读诊断结果（11:00 UTC，用户要求纯只读，不重启/不 kill/不改配置）
- node 实时状态：Win-CAD-Node paired · connected；caps 7/7；node.exe 仅 1 个（PID 15084, Session 3, 11:01:50 启动，ZHANGLIHUA\11561）
- CUA execution 状态：__close_execution 返回 ok:true（无残留 execution）；紧接着 list_apps 立刻 COMPUTER_HOST_BUSY；screen.snapshot 正常（不占 CUA execution 锁）
- 判断：busy 是**并发的 provider execution 锁竞争**（另一个正在调用 CUA 的 agent session 持锁期间，launch_app/list_apps/type 全部 busy；screen.snapshot/close 不持锁所以通），不是 orphaned execution，也不是重复 node.exe
- subagents list = 0 活跃，排除本 session subagent 占用
- launch_app 没打开 Notepad 的真实原因：调用根本没执行到（被 BUSY 挡住），之前"成功"汇报是误判
- CUA provider 锁是 node 侧 in-process 状态，gateway 侧无只读 API 可直接定位具体占用 session ID
- 处置原则：等待占用者自然结束；调用 CUA 前先 __close_execution；若反复 busy 等 30-60s 重试；不重启 node、不改 allowlist、不清 execution/session 数据

### 当前状态
- 截至 11:00 UTC：Win-CAD-Node connected + 7/7 caps，CUA 功能可用但受并发锁间歇影响（busy 时 launch_app 失败，screen.snapshot 不受影响）
- 用户未指示进一步操作；保持只读，等下一步指令

## 21:07 晚检巡检记录

- Ubuntu: load 0.70/0.40/0.21 · mem 1730/3915MB（avail 2185）· / 22% · Docker: homeassistant Up 2 weeks · Gateway active (pid 265183, wss://127.0.0.1:18789) · apt: 29 可升级（未执行升级）
- iStoreOS: load 1.93/1.61/1.41 · sdb4 8% · sda1 30%（均低于 85% 阈值）· Tailscale: istoreos-1 在线（其余2台离线）· OpenClash 正常 · serve 配置存在（SERVE_RUNNING）
- ssl-cert-check: 4 个问题证书全部位于 go/pkg/mod 测试目录（googleapis/certificate-transparency-go testdata），无生产证书过期
- disk-trend-analyze: 趋势数据已更新 (2026-09-21 21:07:52 CST)
- security-mail-check: 扫描完成，无异常邮件
- fail2ban: not running / no bans；SSH 登录仅来自 192.168.100.109 公钥（正常）
- 结论：✅ 全部正常，无需通知

## 21:15 晚检巡检记录（二次确认）

- Ubuntu: load 0.70/0.56/0.33 · mem 1.8Gi/3.8Gi（avail 2.0Gi）· / 22
## 21:15 晚检巡检记录（二次确认）

- Ubuntu: load 0.70/0.56/0.33 · mem 1.8Gi/3.8Gi（avail 2.0Gi）· / 22% · Docker: homeassistant Up 2 weeks · Gateway 18789 监听中 · HA :8123 返回 200
- iStoreOS: load 1.85/1.78/1.54 · sdb4 8% · sda1 30%（均低于 85% 阈值）· OpenClash clash 运行中（pid 775）· Tailscale: istoreos-1 在线（100.123.106.24），istoreos/zhanglihua offline · tailscale0 UP（serve 配置存在）
- ssl-cert-check: 4 个问题证书全部位于 go/pkg/mod 测试目录，无生产证书过期
- disk-trend-analyze: 趋势数据已更新 (2026-09-21 21:12:01 CST)，无分区超阈
- security-mail-check: 扫描完成，无异常邮件
- 结论：✅ 全部正常，无需通知（日报已刷新）

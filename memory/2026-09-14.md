# 2026-09-14 工作日志

## Win-CAD-Node 修复任务（进行中，截至 04:14 UTC 暂停）

**任务目标**：让 Windows CAD Node（Win-CAD-Node, 192.168.100.109）恢复 Gateway 连接，从 `paired · disconnected · unapproved` 变为 `paired · connected`。

### 根因（已确认）
- OpenClaw 2026.6.5 → 2026.9.4 升级时，Win-CAD 的 node-surface 未完整迁移进 device record
- SQLite `device_pairing_paired` 中 Win-CAD 的 `node_surface_json=NULL`，导致 CLI 判 `unapproved`
- 官方 CLI（`nodes approve`）只能批 pending 请求，无 pending → 死锁
- 2026.9.4 的 TLS 端点：Gateway 18789 是 `wss://`（TLS），Windows 注册命令必须用 `--tls`，不能用 `--no-tls`
  - 之前给 `--no-tls` 导致 `socket hang up (1006)`，原因即 transport 不匹配

### 已完成操作
- **阶段 0**：备份 → `/home/ubuntu/.openclaw/backups/wincad-repair-20260914T022401Z/`（7 文件，非空校验通过）
- **阶段 2**：`openclaw nodes remove --node Win-CAD-Node` 成功（旧 ae58d203... 删除）
- **阶段 3（多轮 setup code）**：
  - 第 1 轮：`/j/7iD9...` URL → `Invalid pairing setup code or URL`（原因：格式错，应 `oc-pair://` scheme）
  - 第 2 轮：`oc-pair://` + `--no-tls` → `socket hang up (1006)`（原因：TLS mismatch，Gateway 是 wss）
  - 第 3 轮：`oc-pair://` + `--tls` → 成功到达 **Device Pairing 阶段**，requestId `798f37c2-b51f-40cb-9d55-c45633fe95ef`
- **Device 批准**：`openclaw devices approve 798f37c2...` → 成功，Win-CAD 回到 Paired 状态

### 当前卡点（04:14 UTC 暂停）
- Device 已 paired + approved
- 但 `nodes status` 显示 Win-CAD = `paired · disconnected · unapproved`，caps 为空
- `nodes pending` = 空（无 node-surface 待批请求）
- **需只读审计**：Win-CAD vs Redmi 的 `node_surface_json` 字段对照，确认 node-surface 为何未注册
- 04:05 审计请求被中断，尚未完成

### 关键配置参数
| 项 | 值 |
|---|---|
| Gateway | `192.168.100.108:18789`（wss/TLS） |
| 正确的 Windows 注册命令 | `openclaw node run --pair "oc-pair://<code>" --host 192.168.100.108 --port 18789 --tls --display-name "Win-CAD-Node"` |
| Win-CAD 新 Device ID | ae58d203...（重新生成后同前） |
| 备份目录 | `/home/ubuntu/.openclaw/backups/wincad-repair-20260914T022401Z/` |

### 安全边界（kami 明确批准）
- 允许：Node 重启、pairing 删除重建、identity/token 重新生成、Scheduled Task 修复、Gateway approve
- 禁止：修改 Gateway 配置/SQLite/其他 Node、重新生成多个 setup code、让 Windows 无限重试

### 下一步（恢复时）
1. 完成 Win-CAD vs Redmi 的 `node_surface_json` 字段只读对照
2. 确认 node-surface 是否已自动注册（Windows 重连后）
3. 如 node-surface 已注册 → `nodes approve <requestId>` 完成批准
4. 如未注册 → 等 Windows 侧重连，再查 `nodes pending`
5. 最终目标：`Win-CAD-Node = paired · connected`，caps 非空

## 21:00 晚检巡检（2026-09-14）

**Ubuntu AI Server (192.168.100.108)**
- 系统: up 13d | 负载 0.36-0.59 | 内存 1.1Gi/3.8Gi | / : 22% (74G 可用)
- Docker: homeassistant Up 13 days
- Gateway: OK (74ms) | HA: 200
- 可升级包: base-files, byobu, console-setup, containerd.io, docker 系列（非紧急）

**iStoreOS 路由器 (192.168.100.1)**
- 系统: up 13d | 负载 1.52-1.86
- 磁盘: overlay 73% (只监控) | sdb4 8% | sda1 29%（均在阈值内）
- OpenClash: running | Tailscale: running
- Tailscale serve: 无配置（`tailscale serve status` 返回 "No serve config"）

**SSL 证书**
- gateway 本地证书: 剩余 338 天 / 364 天
- 过期证书仅来自 Go 包测试数据（testdata），忽略

**安全邮件扫描**
- 完成（13:00 CST），无新增高危/警告事件

**磁盘趋势**
- 已采集，iStoreOS overlay 73% 稳定（近 30 分钟无变化）

**巡检结果**: ✅ 全部正常，无异常，不通知
